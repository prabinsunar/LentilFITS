<body class="hold-transition skin-red-light sidebar-mini">
<%= render 'layouts/header' %>
<div class="content-wrapper">

  <!-- Content Header (Page header) -->
  <section class="content-header">
    <h1>
      Edit Donator
    </h1>
    <ol class="breadcrumb">
      <li><a href="#"><i class="fa fa-home"></i> Home Page</a></li>
      <li class="active">Edit Donator</li>
    </ol>
  </section>

  <!-- Main content -->
  <section class="content">
    <div class="row">
      <!-- left column -->
      <div class="col-md-6">
        <!-- general form elements -->
        <div class="box box-header">
          <div class="box-header with-border">
            <h3 class="box-title">Basic Information</h3>
          </div>
          <!-- /.box-header -->
          <!-- form start -->
          <%= form_for(@donator) do |f| %>
            <% if @donator.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@donator.errors.count, "error") %> prohibited this donator from being saved:</h2>

                <ul>
                <% @donator.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
                </ul>
              </div>
            <% end %>

            <div class="box-body">
             <div class="form-group">
                <label>Donator Name</label>
                <%= f.text_field :name, autocomplete: "off", placeholder: "Enter Donator Name", class: "form-control", required: true %>
              </div>
             <div class="form-group">
                <label>Address</label>
                <%= f.text_field :address, autocomplete: "off", placeholder: "Enter Address", class: "form-control", required: true %>
              </div>
              <div class="form-group">
                <label>Phone Number</label>
                <%= f.text_field :phone, autocomplete: "off", placeholder: "Enter Phone Number", class: "form-control" %>
              </div>
            <!-- /.box-body -->
            <div class="box-footer">
              <%= f.submit "Submit", class: "btn btn-primary"%>
            </div>
            <% end %>
        </div>
        <!-- /.box -->
      </div>
      <!--/.col (right) -->
    </div>
    </div>
    <!-- /.row -->
  </section>
  <!-- /.content -->

</div>
<%= render 'layouts/footer' %>
</body>
